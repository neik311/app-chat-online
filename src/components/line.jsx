import { View } from "react-native";

export default function Line() {
  return (
    <View
      style={{
        borderBottomWidth: 1,
        borderBottomColor: "#424242",
        width: 400,
      }}
    />
  );
}
